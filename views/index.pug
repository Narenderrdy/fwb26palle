extends layout

block content
  h1= title
  p I am crazy  #{title}
  button(type="button", class="btn btn-success",onclick="update()") press me for the contents
  div(id = "target") text of display
   a(href='/Hat') Hat page link
  script.  
   var client_count=0;
     function update() {
       client_count++;
       document.getElementById("target").innerHTML=`Value now ${client_count}`;
     }  


